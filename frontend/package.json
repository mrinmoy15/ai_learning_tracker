{
  "name": "ai-learning-tracker-frontend",
  "private": true,
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "tsc --project tsconfig.json && npx serve . -p 3000",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "rimraf": "^5.0.0",
    "serve": "^14.2.4",
    "typescript": "^5.5.4"
  }
}
